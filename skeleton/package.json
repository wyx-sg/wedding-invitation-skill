{
  "name": "my-wedding-invitation",
  "version": "0.1.0",
  "description": "Wedding invitation generated by the wedding-invitation skill.",
  "private": true,
  "type": "module",
  "scripts": {
    "derive": "node scripts/derive.js",
    "build": "node scripts/build.js",
    "render": "node scripts/render.js",
    "all": "npm run derive && npm run build && npm run render"
  }
}
